# Project Progress

## 2026-09-05 10:51 UTC - Supervised agent workflow

- Work unit scope: Added project-level operating instructions for tightly supervised, incremental agent work.
- Files changed: `AGENTS.md`, `project_progress.md`.
- Commands and tests: Confirmed that no existing `AGENTS.md` file was present; no runtime tests were applicable.
- Completed behavior: Future coding agents must select bounded work units, verify each unit, append its outcome to this file, and obtain explicit user permission before beginning the next unit.
- Remaining issues or blockers: None for this work unit.
- Proposed next work unit: Begin the first bounded Phase 1 scaffold task only after explicit user approval.

## 2026-09-05 10:54 UTC - Phase 0 prerequisite preparation (retrospective)

- Work unit scope: Reviewed challenge readiness and prepared the human-owned external prerequisites for Google AI, Snowflake, and GitHub. DNS and deployment-host configuration were intentionally deferred.
- Files changed: `.env.operator.example`, `.gitignore`, and the ignored local `.env.operator`; no credentials or private-key material were added to source-controlled files.
- External resources prepared: The human created a Snowflake Standard trial account and the public GitHub repository `karot00/grant-preflight`. The repository was independently confirmed public and empty through its public GitHub page.
- Snowflake administration completed by the human: Queried account metadata; created the X-Small `GRANT_PREFLIGHT_WH` warehouse with auto-suspend and auto-resume; created the `GRANT_PREFLIGHT` database and `APP` schema; created the `GRANT_PREFLIGHT_APP` least-privilege role; granted warehouse, database, and schema usage; created the `GRANTS` and `ASSESSMENTS` tables; and granted the role `SELECT`, `INSERT`, and `UPDATE` on both tables.
- Snowflake authentication completed by the human: Generated an encrypted PKCS#8 RSA private key outside the repository, generated its public key, created the `GRANT_PREFLIGHT_SERVICE` service user, registered the public key, assigned the application role, and confirmed the user/role configuration in Snowflake. No key contents, account identifiers, or passphrases are recorded here.
- Local operator configuration completed: Added the Snowflake account, service user, private-key path, private-key passphrase, and fixed database/schema/warehouse/role names to the ignored `.env.operator`. Added a Gemini API key and the planned `gemini-3.5-flash` model setting to the same ignored file.
- Commands and tests: Snowflake SQL resource, table, grant, user-description, and role-assignment commands were reported successful by the human. A local non-secret validation loaded `.env.operator`, checked that required Gemini and Snowflake variables were non-empty, confirmed the private-key file existed, and ran `openssl pkey -check -noout`; observed result: `Key is valid`.
- Completed behavior: Local secret storage and cryptographic key loading are ready for the operator path. The planned Snowflake resources, tables, role, and key-pair service user exist. The public challenge repository exists within the challenge window.
- Remaining issues or blockers: A real Python Snowflake Connector login and real Gemini API request have not yet been run. DNS for `grant-preflight.karotammela.fi` is deferred. The Hetzner CX23 host is available over SSH but Docker, Compose, architecture, and ports 80/443 have not yet been verified. The local workspace has not yet been initialized or connected to the public GitHub repository.
- Proposed next work unit: Begin the smallest Phase 1 scaffold unit, including local Git/repository setup and the initial project skeleton, only after explicit user approval.

## 2026-09-05 11:09 UTC - Phase 1 skeleton: Git repository setup and P1.1 file layout

- Work unit scope: The smallest Phase 1 scaffold unit, approved by the user: initialize the local Git repository, connect it to the public GitHub repository, and create the P1.1 file-layout skeleton with honest stubs. No settings, error, model, or service logic was implemented in this unit.
- Files changed: Created the full P1.1 layout: `app.py`, `settings.py`, `models.py`, `errors.py`; `services/` (`__init__.py` plus 8 stub modules); `data/` (5 placeholder JSON files and 5 empty `source_texts/` files); `prompts/` (3 empty files); `sql/schema.sql` (placeholder comment); `scripts/` (`__init__.py`, `smoke_live.py`, `record_demo.py`); `tests/` (`conftest.py` plus 11 docstring-only test files); `.streamlit/config.toml`, `.dockerignore`, `.python-version` (`3.12.14`), `requirements.in`, `requirements-dev.in`, `pytest.ini`, `Dockerfile`, `compose.yaml`, `Caddyfile` (placeholders), `README.md`, `LICENSE` (MIT, "2026 Grant Preflight contributors"), `dev_submission.md` (placeholder). Modified `.gitignore` to add `.kilo/`. `requirements.txt`/`requirements-dev.txt` were intentionally not created; they are generated locks belonging to P1.4.
- Safety note: A GitHub personal access token was found embedded in the local `.kilo/kilo.json` tool configuration, which `.kilo/.gitignore` did not exclude. `.kilo/` was added to the project `.gitignore` before staging, and the staged file list and staged diff were audited to confirm the token was never committed or pushed. The token value is not recorded here. The human should rotate that token and move it out of the JSON config into an environment variable.
- Commands and tests: `git init -b main`; `git remote add origin https://github.com/karot00/grant-preflight.git`; `git add -A`; staged-file audit (61 files; no `.env.operator`, no `.kilo/`, no `*.pem`/`*.p8`); credential-pattern scan of the staged diff (only the empty example variable names and a plan-document code snippet matched; no secrets); `git commit` (root commit `eae4b3e`); `git push -u origin main` (observed: `* [new branch] main -> main`, tracking set, clean status). Verification: `python3 -m compileall -q` over all stub modules → `COMPILEALL OK`; `json.loads` over all five `data/*.json` files → all parsed. Resulting `__pycache__/` directories are gitignored.
- Completed behavior: The workspace is a Git repository on branch `main`, connected and pushed to the public challenge repository `karot00/grant-preflight`. Every unimplemented module raises `NotImplementedError` with its phase reference instead of pretending to succeed. Placeholder data files use the runbook-prescribed top-level shapes (`{"schema_version": 1, ...}`, `is_mock: true`). The three plan files are preserved and committed as project history.
- Remaining issues or blockers: The host Python is 3.12.3, not the required 3.12.14; the P1.4 locking unit must use an exact 3.12.14 interpreter or the pinned `python:3.12.14-slim-bookworm` container for the venv and lock generation, as the runbook prescribes. Hashed lock files do not exist yet. Settings/error contracts are not implemented. Rotating the exposed `.kilo` GitHub token is a human task.
- Proposed next work unit: Implement the P1.3 error contract (`errors.py`: `AppError` with the fixed code allowlist) and the P1.2 configuration contract (`settings.py`: `load_settings`, frozen `Settings` dataclass with secret-safe repr, `PROJECT_ROOT`, mode-combination validation, public-mode credential rejection) together with `tests/test_settings.py`, only after explicit user approval.

## 2026-09-05 11:20 UTC - Phase 1: P1.3 error contract and P1.2 configuration contract

- Work unit scope: User-approved unit implementing `errors.py` (P1.3) and `settings.py` (P1.2) with their initial tests. Dependency locking (P1.4) and the conftest isolation guard (P1.5) were not part of this unit.
- Files changed: `errors.py` (AppError exception with the fixed 14-code allowlist, ValueError on unknown codes, safe-message contract); `settings.py` (frozen `Settings` dataclass with snake_case environment attribute names, secret fields excluded from repr, `PROJECT_ROOT`, `load_settings(environ)` with enum/fixed-value/base-URL validation, public-demo credential rejection, operator-mode combination rules, absolute private-key-path check, credential-completeness flags); `tests/test_settings.py` (50 tests covering both contracts); `pytest.ini` (added `pythonpath = .` so tests import root modules).
- Commands and tests: `python3 -m pytest` → observed `50 passed in 0.08s` (run on system Python 3.12.3 with pytest 9.0.3; the pinned 3.12.14/pytest 9.1.1 environment is a P1.4 gate, not yet installed). `python3 -m compileall -q settings.py errors.py tests/test_settings.py` → OK.
- Completed behavior: `load_settings` accepts only the prescribed mode combinations; public mode rejects non-empty Gemini/Snowflake credentials, naming the variables without echoing values; operator mode rejects recorded-AI with Snowflake; wrong fixed resource names and any non-exact `GEMINI_MODEL` are `CONFIG_INVALID`; empty secrets become `None`; limits and schema/prompt versions are code constants unaffected by environment variables; settings reads only the supplied mapping, never `os.environ`; `AppError` enforces the code allowlist at construction.
- Remaining issues or blockers: The host has no Python 3.12.14 interpreter, no pinned pytest 9.1.1, no uv, and no local Docker; P1.4 must resolve an exact 3.12.14 runtime (for example a pyenv/deadsnakes build locally or the pinned container on the deployment host) before generating hashed locks. The P1.5 outbound-call guard in `tests/conftest.py` is not yet installed. Rotating the previously noted `.kilo` GitHub token remains a human task.
- Proposed next work unit: Implement the P1.5 test-isolation guard in `tests/conftest.py` (public/recorded/memory defaults, cleared inherited credential variables, blocked Requests network operations, real DNS lookups, Gemini client construction, and Snowflake connection creation) and verify the full suite still passes, only after explicit user approval. P1.4 dependency locking follows once the 3.12.14 interpreter question is resolved.

## 2026-09-05 11:50 UTC - Phase 1: P1.4 toolchain and hashed dependency locks

- Work unit scope: User-approved P1.4 unit using option 1 from the blocker discussion (uv-managed toolchain): install uv and standalone CPython 3.12.14, create `.venv`, pin pip/pip-tools, generate both hashed locks, install them, and run the plan's verification gates. The Docker-host image verification part of P1.4 was not in this local unit and remains deferred.
- Files changed: `requirements.txt` and `requirements-dev.txt` (new, generated full transitive locks; committed per the plan); `project_progress.md`. No source files changed. `.venv/` is gitignored.
- Toolchain installed: uv 0.12.10 via the official installer into `~/.local/bin`; `uv python install 3.12.14` → `cpython-3.12.14-linux-x86_64-gnu`; `uv venv --seed --python 3.12.14 .venv`; `.venv/bin/python --version` → `Python 3.12.14`; `pip==26.2.1` and `pip-tools==7.6.1` installed into `.venv` exactly as prescribed.
- Commands and tests: (1) `piptools compile` for the runtime lock with the plan's exact flags (`--resolver=backtracking --allow-unsafe --strip-extras --generate-hashes --pip-args='--only-binary=:all:'`) → exit 0; 67 packages, all six direct pins exact, 1146 hash lines, `--only-binary :all:` carried into the lock. The first foreground attempt was aborted by a 10-minute tool timeout while still resolving; the identical command completed in a tracked background run (~9 minutes) with no deviation from the plan. (2) `piptools compile` for the dev lock, run sequentially after the runtime lock → exit 0; `pytest==9.1.1` pinned. (3) `.venv` install of `requirements-dev.txt` with `--only-binary=:all: --require-hashes` → success; `pip check` → `No broken requirements found.` (4) `.venv/bin/python -m pytest` → `50 passed in 0.06s` on the pinned toolchain (Python 3.12.14, pytest 9.1.1). (5) `compileall` over app/settings/models/errors/services/scripts/tests → OK. (6) Fresh-environment runtime-lock verification per the plan: a new 3.12.14 venv under `/tmp` with only `pip==26.2.1`; `pip install --only-binary=:all: --require-hashes -r requirements.txt` → success; `pip check` → no broken requirements; importing `streamlit`, `google.genai`, `pydantic`, `snowflake.connector`, `requests`, `bs4` → OK; exit 0. Temporary verification environments were deleted afterwards.
- Completed behavior: All local P1.4 gates pass: exact 3.12.14 interpreter, pinned pip/pip-tools pair, hashed transitive locks generated and committed, clean wheels-only hashed installation in both the dev environment and a fresh runtime environment, `pip check` clean in both, and the full test suite green on the pinned toolchain.
- Remaining issues or blockers: The P1.4 Docker-host steps (`docker version`, `docker compose version`, pulling the two digest-qualified images) were not run — no local Docker exists and the Hetzner host's Docker is still unverified per the Phase 0 notes; this remains a named blocked task for deployment-host verification before Phase 6. Rotating the previously noted `.kilo` GitHub token remains a human task.
- Proposed next work unit: Implement the P1.5 outbound-call guard in `tests/conftest.py` (public/recorded/memory test defaults, cleared inherited credential variables, blocked Requests network operations, real DNS lookups, Gemini client construction, and Snowflake connection creation, plus guard self-tests) and verify the full suite passes under `.venv`, only after explicit user approval.

## 2026-09-05 12:15 UTC - Phase 1: P1.5 outbound-call test-isolation guard

- Work unit scope: User-approved P1.5 unit: install the test-isolation guard in `tests/conftest.py` before any service tests exist, and add guard self-tests. No service, model, or settings logic was changed.
- Files changed: `tests/conftest.py` (autouse `outbound_call_guard` fixture: blocks `requests.sessions.Session.send`, `socket.getaddrinfo`/`gethostbyname`/`gethostbyname_ex`, `google.genai.Client.__init__`, and `snowflake.connector.connect`/`SnowflakeConnection.__init__` via a distinct `OutboundCallBlocked(RuntimeError)`; clears inherited credential variables `GEMINI_API_KEY`, `SNOWFLAKE_ACCOUNT`, `SNOWFLAKE_USER`, `SNOWFLAKE_PRIVATE_KEY_FILE`, `SNOWFLAKE_PRIVATE_KEY_PASSPHRASE` and mode variables `APP_MODE`, `AI_MODE`, `STORAGE_MODE` from the process environment; adds a `default_settings` fixture returning public/recorded/memory settings from an empty environment); `tests/test_settings.py` (25 new guard self-tests: blocked Requests calls, blocked DNS lookups, blocked Gemini client construction, blocked Snowflake connection creation, cleared inherited environment variables with per-test monkeypatch injection still possible, guard/settings credential-name consistency, `default_settings` fixture contract, implemented modules import without outbound calls, and all ten unimplemented stub modules raise `NotImplementedError` — never `OutboundCallBlocked` or silent success); `project_progress.md`.
- Commands and tests: `.venv/bin/python -m pytest -q` on the pinned toolchain (Python 3.12.14, pytest 9.1.1) → observed `75 passed in 0.09s` (50 prior + 25 new). `.venv/bin/python -m compileall -q tests/conftest.py tests/test_settings.py` → OK.
- Completed behavior: No Phase 2-4 test can accidentally use developer credentials or reach real network services: every test runs under the guard, inherited credentials are removed from the environment, and any outbound Requests/DNS/Gemini/Snowflake attempt raises `OutboundCallBlocked`. Individual tests can still inject fakes explicitly with their own `monkeypatch` after the guard is installed (verified by self-test). The P1.5 exit-check item "importing settings/models/services performs no network call" is enforced for the implemented modules (`settings`, `errors`); the Phase 2/3 stub modules cannot be imported yet and are verified to fail honestly with `NotImplementedError` instead.
- Remaining issues or blockers: The stub-import self-tests must be updated as each phase replaces a stub with a real module. The P1.4 Docker-host verification steps (`docker version`, `docker compose version`, pulling the two digest-qualified images) remain a named blocked task pending deployment-host access. Rotating the previously noted `.kilo` GitHub token remains a human task. Phase 1 exit checks are otherwise complete.
- Proposed next work unit: Begin Phase 2 with the smallest bounded unit: P2.1/P2.2 shared serialization rules and the exact record models in `models.py` (string-literal enums, strict typing, `ConfigDict(extra="forbid")`, UTC timestamp normalization, bounded text/list constraints) together with `tests/test_models.py`, only after explicit user approval.

## 2026-09-05 12:28 UTC - Phase 2: P2.1 serialization rules and P2.2 record models

- Work unit scope: User-approved unit (commit P1.5, continue): implement the shared Pydantic record models and string-literal enums in `models.py` with `tests/test_models.py`. Hashing/IDs (P2.3), fixtures (P2.4-P2.6), and all services remain out of scope.
- Files changed: `models.py` (replaced the Phase 1 stub): eight `StrEnum` enums with the exact prescribed values (`SourceKind`, `ReviewStatus`, `Decision`, `MissionFit`, `Dimension`, `DeadlineKind`, `DraftKind`, `OutputOrigin`); annotated field types (`SchemaVersion` = before-validator rejecting bools/non-ints plus `Literal[1]`; `UtcDatetime` rejecting naive timestamps and normalizing to UTC; `CurrencyCode` `^[A-Z]{3}$`; `Money`/`TokenCount` non-negative `StrictInt`; `WordLimit` positive `StrictInt`); `StrictRecord` base with `ConfigDict(extra="forbid")`; all P2.2 records (`Fact`, `BudgetLine`, `OrganizationProfile`, `SourceSnapshot`, `QuotedValue`, `Deadline`, `ExtractedRequirement`, `ApplicationSection`, `ExtractionResult`, `Requirement`, `GenerationMeta`, `Grant`, `RequirementReview`, `ReviewSet`, `Assessment`, `DraftSection`, `Draft`, `DraftSectionResult`, `DraftResult`, `AssessmentRecord`); constants `MAX_PROFILE_TEXT_CHARS=10000`, `MAX_PROFILE_FACTS=50`, `MAX_PROFILE_BUDGET_LINES=20`, `DEMO_FIXTURE_IDS` (the five case names). Validators: unique fact/requirement/review/section IDs; assessment review IDs exactly match snapshot requirement IDs; `amount_min_minor <= amount_max_minor`; `fetched_at` only with `kind="fetched"`; synthetic sources require a known fixture ID, no source URL, and `is_synthetic=true`; fetched/pasted have `fixture_id=None`; source text bounded by `MAX_SOURCE_CHARS`; profile bounds raise `AppError("INPUT_INVALID", ...)` (verified to propagate unwrapped through Pydantic); `at` only for datetime deadlines, `on` only for date deadlines; authored `GenerationMeta` claims no model/response ID. Provider-output schemas keep every field required (explicit empty lists/nulls); application-owned lists/dicts use `default_factory`. The 40-requirement/12-section limits are deliberately NOT model constraints so P3B.2 can raise `AI_LIMIT` instead of a generic list-length failure. `tests/test_models.py` (new, 87 tests with in-memory factories); `tests/test_settings.py` (moved `models` from the unimplemented-stub list to the implemented-import guard test, as anticipated by the P1.5 entry); `project_progress.md`.
- Commands and tests: `.venv/bin/python -m pytest -q` on the pinned toolchain (Python 3.12.14, pytest 9.1.1) → observed `162 passed in 0.23s` (75 prior + 87 new). `.venv/bin/python -m compileall -q models.py tests/test_models.py tests/test_settings.py` → OK. Pre-implementation behavior probes on the pinned Pydantic 2.13.5 confirmed: `AppError` propagates unwrapped from validators; `Literal[1]` with the before-validator rejects `True`, `"1"`, `1.0`, `2`; `StrictInt`/`StrictBool` reject floats/bools/strings; `StrEnum` dumps as plain JSON strings; aware datetimes parse from JSON alongside strict fields.
- Completed behavior: All P2.2 records round-trip through JSON (`model_dump(mode="json")` / `model_validate_json`) with snake_case keys, plain enum strings, and UTC timestamps; `true` is rejected as money, `400000.0` as an integer amount, and `"false"` as a boolean; naive timestamps are rejected and aware ones normalized to UTC; unknown fields are rejected everywhere; the full `AssessmentRecord` graph (assessment + grant + source + extraction + reviews + draft) round-trips equal. The P1.5 guard remains active over all model tests.
- Remaining issues or blockers: The Phase 2 exit check "hashes are stable across dictionary ordering" belongs to P2.3 (`services/evidence.py`) and is still pending; the model-level dump-order stability test is a placeholder for that gate. `Requirement` inherits `ExtractedRequirement`, so provider responses are validated before application assembly assigns `id`/`evidence_valid` (P2.3 `validate_extraction`). The Docker-host verification and `.kilo` token rotation remain outstanding human/deployment tasks.
- Proposed next work unit: P2.3 canonical hashing and IDs in `services/evidence.py` (`normalize_for_quote`, `hash_source`, `hash_profile`, `hash_reviews`, `hash_extraction`, `grant_id`, `draft_fingerprint`, `validate_extraction`) with `tests/test_evidence.py`, only after explicit user approval.

## 2026-09-05 12:40 UTC - Phase 2: P2.3 canonical hashing, IDs, and extraction validation

- Work unit scope: User-approved unit: implement the pure evidence helpers in `services/evidence.py` with `tests/test_evidence.py`. Fixtures (P2.4-P2.6) and all other services remain out of scope.
- Files changed: `services/evidence.py` (replaced the Phase 1 stub): `normalize_for_quote` (exactly `" ".join(text.split())`, case/punctuation preserved); `hash_source` (SHA-256 of normalized UTF-8 bytes); canonical JSON hashing via `model_dump(mode="json")` + `json.dumps(sort_keys=True, separators=(",",":"), ensure_ascii=False)` + SHA-256; `hash_profile` (all fields and approval flags, facts sorted by ID, no timestamps); `hash_reviews` (all flags/statuses/fit decisions, items sorted by requirement ID); `hash_extraction` (requirement/section order preserved); `grant_id` (`uuid.uuid5(NAMESPACE_URL, "grant-preflight:" + key)`; key = `fixture:<id>` for synthetic, canonical fragment-stripped URL for fetched/pasted URL sources, `text:<source_hash>` without a URL; identity read from the shared source model); `draft_fingerprint` (grant ID, source hash, extraction hash, profile hash, review hash, draft kind, model ID, extraction prompt version from the grant meta, `DRAFT_PROMPT_VERSION` from settings; excludes event IDs, origin labels, response IDs, and timestamps; `ValueError` on unknown kind); `validate_extraction` (replaces section IDs with `S001`... in supplied order before storing the normalized `ExtractionResult` in `Grant.extraction`, assigns requirement IDs `R001`... in extraction order, sets `evidence_valid` and `metadata_evidence_valid` for the fixed keys `foundation`/`title`/`amount`/`deadline` plus `section:S###` — true only for nonempty whitespace-normalized case-sensitive substring matches, never erases invalid requirements, downgrades suggestions with unknown fact references to `unknown` keeping only known IDs, sets `extraction_profile_hash`, passes meta through, and stamps `created_at`/`updated_at` from `now`). `tests/test_evidence.py` (new, 41 tests using the fixed clock `2026-09-05T12:00:00Z`); `tests/test_settings.py` (moved `services.evidence` from the unimplemented-stub list to the implemented-import guard test); `project_progress.md`.
- Commands and tests: `.venv/bin/python -m pytest -q` on the pinned toolchain (Python 3.12.14, pytest 9.1.1) → observed `203 passed in 0.24s` (162 prior + 41 new), no warnings. `.venv/bin/python -m compileall -q services/evidence.py tests/test_evidence.py tests/test_settings.py` → OK.
- Completed behavior: Hashes are deterministic and stable across fact/item ordering and construction ordering while sensitive to every field, flag, approval, and meaningful list order; whitespace-only quote normalization behaves as specified (whitespace-insensitive, case-sensitive, empty quotes never match); `grant_id` is a stable UUID5 per source identity (all five fixture IDs distinct; fragment stripped, query preserved); `draft_fingerprint` changes with every included component and is identical across origin labels, response IDs, and timestamps, so a replay matches equivalent reviewed inputs but a different extraction of the same source cannot reuse an old draft; `validate_extraction` is idempotent for already-normalized section IDs (the recorded-provider boundary) and the assembled `Grant` round-trips through JSON.
- Remaining issues or blockers: The Phase 2 exit check "five cases validate" depends on the P2.4-P2.6 fixtures, which do not exist yet. `validate_extraction` does not verify `source.source_hash` against `hash_source(source.text)`; exact source text/hash matching is a P2.5 recording-bundle check. The Docker-host verification and `.kilo` token rotation remain outstanding human/deployment tasks.
- Proposed next work unit: P2.4 fixed organization fixture: author `data/demo_profile.json` and `data/salesforce_npsp_data.json` per the runbook, implement the deterministic history mapping in `services/salesforce_service.py` and `build_demo_profile(*, approved: bool)` in `services/demo_service.py`, with `tests/test_salesforce.py` and the profile-builder part of `tests/test_demo.py`, only after explicit user approval.
