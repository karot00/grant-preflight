"""Pure Python decision engine and deadline rules.

Implemented in Phase 3, Lane B (P3B.4-P3B.5): ``evaluate(grant, profile,
reviews, as_of, assessment_id) -> Assessment`` with the fixed
skip/clarify/pursue precedence. The model never sets the final decision.
"""

raise NotImplementedError(
    "services/assessment.py is implemented in Phase 3, Lane B"
)
