"""Application settings contract.

Implemented in Phase 1 (P1.2): ``load_settings(environ: Mapping[str, str]) ->
Settings`` with the frozen ``Settings`` dataclass, ``PROJECT_ROOT``, mode
validation, and fixed Snowflake resource names.
"""

raise NotImplementedError("settings.py is implemented in Phase 1, P1.2")
