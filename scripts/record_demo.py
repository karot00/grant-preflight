"""Public demo recording script (``python -m scripts.record_demo``).

Implemented in Phase 5 (P5.3). Loads an operator record by assessment ID,
applies all exact-baseline/bundle checks from P2.5, and writes candidate
recordings into ignored local scratch space for human review before they are
moved into ``data/demo_recordings.json``.
"""

raise NotImplementedError("scripts/record_demo.py is implemented in Phase 5, P5.3")
